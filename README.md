# CampusRoomFinder

## Requirements

- JDK 26
- IntelliJ IDEA 2025+
- Maven (or use the Maven Wrapper)

## Setup

1. Clone the repository.
2. Open `Roomify/pom.xml` as a Maven project.
3. Set Project SDK to JDK 26 if prompted.
4. Wait for Maven to download dependencies.
5. Run `HelloApplication`.
